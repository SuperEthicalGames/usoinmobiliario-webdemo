# Variables NO sensibles para producción (Cloud Functions, proyecto usoinmobiliario-c8e83).
# Firebase carga este archivo automáticamente por su nombre (convención .env.<project-id>) —
# a diferencia de ".env" (ignorado por git, solo local), ESTE archivo SÍ se sube al repo porque
# no contiene secretos. Los valores sensibles (tokens/API keys) van aparte, por Secret Manager:
# ver README.md — "firebase functions:secrets:set NOMBRE".

FIREBASE_DATABASE_URL=https://usoinmobiliario-c8e83-default-rtdb.firebaseio.com

# TODO: reemplazar por el phone_number_id del número de WhatsApp Business REAL de producción
# (Meta for Developers > tu app > WhatsApp > API Setup) antes del primer deploy a producción.
WHATSAPP_PHONE_NUMBER_ID=

WHATSAPP_API_VERSION=v20.0

LLM_PROVIDER=gemini
GEMINI_MODEL=gemini-flash-lite-latest
OPENAI_MODEL=gpt-4o-mini

SITE_BASE_URL=https://superethicalgames.github.io/usoinmobiliariodemo

GMAIL_USER=usoinmobiliario@gmail.com
